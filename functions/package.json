{
  "name": "brainrot-labs-functions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --format=cjs --outfile=dist/index.js --external:firebase-admin --external:firebase-functions",
    "build:watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@google/genai": "^1.46.0",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "esbuild": "^0.25.12",
    "typescript": "~5.8.2"
  }
}
